version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8
    commands:
      - echo Installing necessary dependencies..
  pre_build:
    commands:
    - echo In the pre_build phase of sample app
    - pwd
    - ls -lrt
  build:
    commands:
    - echo Build started on `date`
  post_build:
    commands:
    - echo Build completed on `date`